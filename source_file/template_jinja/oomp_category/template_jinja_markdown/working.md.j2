# {{ p.name_short | default(p.name_proper, true) }}

`{{ p.part_page.oomp_id }}`

{{ p.part_page.summary }}

<p align="center">
  <a href="{{ p.part_page.main_image.svg }}">
    <img src="{{ p.part_page.main_image.svg }}" alt="{{ p.name_short | default(p.name_proper, true) }} pinout" width="620">
  </a>
  <br>
  <strong>{{ p.part_page.main_image.title }}</strong> · click for the full-size SVG
</p>

## At a glance

| Detail | Value |
| --- | --- |
{% for fact in p.part_page.quick_facts -%}
| {{ fact.title }} | {% if fact.title == 'OOMP ID' %}`{{ fact.value }}`{% else %}{{ fact.value }}{% endif %} |
{% endfor %}

## Diagram gallery

<table>
{% for preview_row in p.part_page.preview_rows %}
  <tr>
  {% for diagram in preview_row %}
    <td align="center" width="33%">
      <a href="{{ diagram.svg }}"><img src="{{ diagram.preview }}" alt="{{ diagram.title }}" width="220"></a><br>
      <strong>{{ diagram.title }}</strong>
    </td>
  {% endfor %}
  </tr>
{% endfor %}
</table>

Each preview links to its full-size vector drawing.

## Classification

| Level | Value |
| --- | --- |
{% for taxonomy in p.part_page.taxonomy -%}
| {{ taxonomy.level }} | `{{ taxonomy.value }}` |
{% endfor %}

{% if p.dimensions_mm is defined and p.dimensions_mm %}
## Nominal dimensions

| Measurement | Value |
| --- | ---: |
{% for dimension_name, dimension_value in p.dimensions_mm.items() -%}
| {{ dimension_name | replace('_', ' ') | title }} | {{ dimension_value }} mm |
{% endfor %}
{% endif %}

{% if p.part_page.identifiers %}
## Identifiers

| Identifier | Value |
| --- | --- |
{% for identifier in p.part_page.identifiers -%}
| {{ identifier.title }} | `{{ identifier.value }}` |
{% endfor %}
{% endif %}

{% if p.part_page.pins %}
## Pins

| Pin | Name | Type |
| ---: | --- | --- |
{% for pin in p.part_page.pins -%}
| {{ pin.number }} | {{ pin.name }} | {{ pin.type }} |
{% endfor %}
{% endif %}

## Files

- [Pinout drawing](working_svg_square_pins.svg)
- [Assembly drawing](working_svg_assembly.svg)
- [Outline drawing](working_svg_outline.svg)
- [Part ID drawing](working_svg_part_id.svg)
- [MD5 alpha drawing](working_svg_md5_6_alpha.svg)
- [BIP 39 words drawing](working_svg_bip_39_3_word.svg)
- [Square summary](working_svg_square.svg)
- [Dimensioned drawing](working_svg_dimensioned_titles.svg)
{% if p.part_page.has_datasheet %}
- [Datasheet](datasheet.pdf)
{% endif %}
- [Structured definition](working.yaml)

{% if p.part_page.repository_url %}
[View this part on GitHub]({{ p.part_page.repository_url }})
{% endif %}

---

This page is generated from the OOMP part definition by a Roboclick Jinja template. Edit the source taxonomy or template rather than this generated file.
